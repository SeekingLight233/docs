---
title: javascript常用设计模式
date: 2019-9-9
categories: 
 - 前端
---
## 工厂模式
借助于一个“工厂”创建出某个产品的实例，具体这个产品怎样复杂，用户并不关心，用户只要这个产品的实例。

而“工厂”的作用就是为了隐藏创建产品的复杂度，只保留对外的接口。
``` js
class Product {
    constructor(name) {
        this.name = name;
    }
    init() {
        console.log("init!")
    }
    fn1() {
        console.log("fn1!")
    }
    fn2() {
        console.log("fn2!")
    }
}

class Creator {
    creat(name) {
        return new Product(name)
    }
};
//test
let p = new Creator().creat("p1");
p.init()
```
## 单例模式
单例模式的核心就是：一个类只允许有一个有效的实例。
由于JS语言的限制，我们只能用闭包来实现。
``` js
class SingleObj {
    constructor() {
        console.log("constructor!")
    }
}
//为SingleObj上创建一个静态的，自执行的方法
SingleObj.getIns = (function() {
    let instance = null;
    //返回一个闭包
    return function() {
        if (!instance) {
            instance = new SingleObj();
        }
        return instance;
    }
})();

//test
let ins1 = SingleObj.getIns();
let ins2 = SingleObj.getIns();
console.log(ins1 === ins2); //true
```
这种模式在Vuex这种全局状态管理的库中用的非常多。
## 适配器模式
适配器模式比较好理解，如果有一个接口与我们的系统不兼容，那么我们只需要给这个接口加个适配器，从而达到转换的效果。
``` js
class Plug {
    getInfo() {
        console.log("这是一个港版的插头");
    }
}
class Adapter {
    constructor() {
        this.plug = new Plug();
    }
    transform() {
        //在这里对this.plug进行转换操作
    }
}
```
比如Vue中的`computed`就可以为我们提供“适配器”的效果。
## 装饰模式
装饰模式可以在不改变接口的条件下为对象添加新的功能。
Q：这里有疑问，为什么ES的提案中有装饰器的语法呢？直接继承写拓展不好吗？？？
## 代理模式

## 发布-订阅模式